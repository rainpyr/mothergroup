<h2>All Activity Categories</h2>

<p>
Total: <%= pluralize(@categories.length, 'category') %>
</p>

    <ul>
    <% @categories.each do |category| %>
        <li>
            <%= link_to category.name, category_path(category) %>
            
        </li>
        <% end %>
    
    </ul>
